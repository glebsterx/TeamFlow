# TeamFlow — Предложения по развитию

> Составлено 2026-06-13 по итогам аудита (v0.8.25). Дополняет `ROADMAP.md`.
> Приоритет: 🔴 высокий / 🟠 средний / 🟡 низкий. Усилие: S (часы) / M (день-два) / L (неделя+).
>
> **Обновление 24.08.2026:** из раздела 1 и 3 закрыто — персистентные напоминания, CI (без
> Actions), healthcheck, авторизация записи, разбивка `Dashboard.tsx`, лишний ruff-долг в
> routes.py, утечка пароля прокси в логи (`proxy_applied`/`unknown_proxy_scheme` теперь маскируют
> `user:pass@`). Остальное — разделы 2 (частично), 4, 5, 6 — по-прежнему открыто, см. отметки ниже.

---

## 1. Надёжность и инфраструктура

- [x] 🔴 **S — Персистентные напоминания (закрывает AUD-8).** Закрыто 23.08 — таблица `reminders` + `restore_reminders()` при старте (PLAN.md FIX-26).

- [x] 🔴 **M — CI.** Закрыто 24.08 — без GitHub Actions (тут только хранилище кода): `scripts/check.sh` + `.githooks/pre-push` (PLAN.md FIX-19).

- 🟠 **S — Расхождение docs vs реальность по миграциям.**
  CLAUDE.md/ARCHITECTURE заявляют Alembic, фактически — ручной идемпотентный список в `migrate.py`. Либо внедрить Alembic, либо зафиксировать в доке, что источник истины — `migrate.py` (чтобы фичи вроде `deleted_at` не уезжали в прод без записи туда — это и был AUD-2).
  **Закрыто** — CLAUDE.md переписан под реальность (PLAN.md FIX-22), ARCHITECTURE.md Alembic/Postgres не упоминает (проверено 24.08).

- [x] 🟠 **S — Docker healthcheck.** Закрыто 24.08 — добавлен и backend, и frontend (PLAN.md FIX-29).

- [x] 🟡 **S — Ротация бэкапов и алерт при сбое.** Закрыто 24.08. Ротация (`MAX_BACKUPS=10`) уже
  была, но бэкап делался только один раз при старте процесса — на стабильном проде без рестартов
  недели подряд это значит вообще ни одного свежего бэкапа. Добавлен `app/telegram/backup_scheduler.py`
  — ежедневный бэкап фоновой задачей (как `deadline_notifier`), при ошибке шлёт алерт в Telegram
  всем `system_role='admin'` с привязанным Telegram. `backup_database()` получил `raise_on_error`
  (по умолчанию False — при старте сбой бэкапа не должен ронять запуск).

## 2. Качество кода (технический долг)

- [x] 🟠 **L — Разбить `Dashboard.tsx` и `routes.py`.**
  `Dashboard.tsx` 1340→1055 строк — 4 из 6 режимов просмотра задач вынесены (PLAN.md FIX-27, 24.08); header/toolbar/оркестрация модалок остались — дальше нужен Context или prop-drilling через весь файл, отдельная задача.
  `routes.py` — закрыто 24.08: 3986→1960 строк, вынесены `routes_knowledge_base.py`,
  `routes_meetings.py`, `routes_projects.py`, `routes_sprints.py` (спринты жили в двух разных
  местах файла — теперь в одном), `routes_push.py`, `routes_ai.py`; часть `/settings/*` — в уже
  существующий `routes_system_settings.py`. Чисто механический перенос, список из 97 роутов
  (метод+путь) до/после идентичен, `/bot-status` и `/api-keys*` намеренно не переехали под
  `/api/settings` (сменило бы их реальный URL).

- 🟠 **S — Единый паттерн в роутерах (#313).** Не начато — смесь raw SQL/repo/service остаётся, признано приемлемым для команды 2-5 чел на момент написания, не переоценивалось.

- [x] 🟡 **S — Подчистить ruff.** Закрыто 24.08 — `ruff check app` чист кроме намеренно исключённых E711/E712/E402 (SQLAlchemy `==`/circular imports, см. `backend/pyproject.toml`). Локальные импорты (#314, ~99 мест) не выносились на уровень модуля — часть из них E402-исключения именно из-за этого.

## 3. Безопасность

- [x] 🔴 **M — Закрыть запись без авторизации.** Закрыто 24.08 — JWT на всех API-роутах кроме login/register/OAuth/health/webapp (PLAN.md FIX-2). Остаточный IDOR: `GET /auth/google/link`/`yandex/link` всё ещё принимают `account_id` через query (редирект по `window.location.href`, приложить заголовок нельзя без signed-link на фронте) — см. ROADMAP.md «Технический долг».

- [x] 🟠 **S — Rate-limit на AI-эндпоинты.** Закрыто 24.08 — `app/core/rate_limit.py`, in-memory sliding window per-account (10/мин на `/ai/parse` и `/ai/suggest-tags`, 20/мин на `/ai/models`; 429 + `Retry-After`). Тесты в `tests/test_ai.py`.

- [x] 🟡 **S — Аудит секретов в логах.** Закрыто 24.08 — `proxy_applied`/`unknown_proxy_scheme` маскируют `user:pass@` перед логированием (`_mask_proxy_creds` в `bot.py`).

## 4. Telegram-first фичи (сильная сторона продукта)

- 🔴 **M — Inline-кнопки действий прямо в чате.**
  Под задачей в боте — кнопки «Взять», «В работу», «Готово», «+1ч», «Напомнить». Callback уже есть в проекте; это резко ускоряет основной сценарий маленькой команды без захода в веб.

- 🟠 **M — Плановый авто-дайджест в Telegram.**
  Дайджест уже считается для UI/CSV. Добавить расписание (ежедневно утром / по понедельникам) с пушем в чат команды. APScheduler или существующий планировщик.

- 🟠 **S — @упоминания в комментариях → уведомление.**
  При `@username` в комментарии слать пуш/Telegram упомянутому. Дёшево, заметно повышает вовлечённость.

## 5. Продуктовые расширения

- 🟠 **M — Полнотекстовый поиск по Базе знаний (SQLite FTS5).**
  БЗ растёт (папки/страницы/корзина уже есть) — нужен поиск по содержимому, а не только по задачам.

- 🟠 **M — Чеклисты внутри задачи (#47 из backlog).**
  Лёгкие подпункты без создания полноценных подзадач — частый запрос для мелких шагов.

- 🟠 **M — Аналитика: burndown + velocity.**
  Данные спринтов и `completed_at` уже есть. Небольшой график сгорания и скорости команды — высокая ценность при низкой стоимости (данные собраны).

- 🟡 **L — Вложения к задачам (#30, без OCR на старте).**
  Файлы/скриншоты к задаче. OCR — отдельной итерацией.

- 🟡 **L — Голосовой ввод задач (#19/#28).**
  Voice → текст (через ту же AI-интеграцию) → авто-парсинг в задачу. Естественно ложится на Telegram.

## 6. Часовые пояса (#319 — связано с найденным багом)

- 🔴 **M — Завершить рефактор TZ.**
  В `/remind` MSK зашит как `+3` (AUD-4). Перенести TZ в аккаунт пользователя, единый `Clock.now()`, UI выбора. Убирает класс багов «время не в том поясе» во всём боте и дайджестах.

---

## Рекомендуемый ближайший порядок
1. 🔴 CI (GitHub Actions) — страхует всё остальное.
2. 🔴 Персистентные напоминания (AUD-8) + #319 TZ — закрывают реальные баги.
3. 🔴 Закрыть write-эндпоинты авторизацией + rate-limit AI.
4. 🟠 Inline-кнопки в боте + авто-дайджест — максимум продуктовой ценности на Telegram-first.
5. 🟠 Разбивка `routes.py` / `Dashboard.tsx` — когда накопится трение от монолитов.
