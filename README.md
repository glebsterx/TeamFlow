# TaskFlow MVP - Team Task Management

Современный инструмент управления задачами для малых команд (2-5 человек).

## 🚀 Технологический стек

### Backend
- **Python 3.11** с FastAPI
- **PostgreSQL** для хранения данных
- **SQLAlchemy** ORM
- **JWT** аутентификация
- **Pydantic** валидация

### Frontend
- **React 18** с TypeScript
- **Vite** сборщик
- **TanStack Query** управление сервером
- **Zustand** локальное состояние
- **Tailwind CSS** стилизация

## 📋 Возможности MVP

✅ Регистрация и аутентификация пользователей  
✅ Создание, редактирование и удаление задач  
✅ Назначение задач участникам команды  
✅ Статусы задач: To Do, In Progress, Done  
✅ Приоритеты: Low, Medium, High, Urgent  
✅ Фильтрация задач по статусу  
✅ Срок выполнения задач  
✅ Просмотр всех задач команды  

## 🛠️ Установка и запуск

### Предварительные требования

- Docker и Docker Compose
- (Опционально) Node.js 20+ и Python 3.11+ для локальной разработки

### Быстрый старт с Docker

1. **Клонируйте репозиторий**
```bash
git clone <repository-url>
cd taskflow-mvp
```

2. **Запустите все сервисы**
```bash
docker-compose up --build
```

3. **Откройте приложение**
- Frontend: http://localhost:5173
- Backend API: http://localhost:8000
- API Docs: http://localhost:8000/docs

### Первый запуск

1. Создайте первого пользователя через регистрацию на frontend
2. Войдите в систему
3. Начните создавать задачи!

## 📁 Структура проекта

```
taskflow-mvp/
├── backend/              # FastAPI бэкенд
│   ├── app/
│   │   ├── api/         # API endpoints
│   │   ├── core/        # Конфигурация, безопасность, DB
│   │   ├── models/      # SQLAlchemy модели
│   │   ├── schemas/     # Pydantic схемы
│   │   ├── services/    # Бизнес-логика
│   │   └── main.py      # Точка входа
│   ├── requirements.txt
│   └── Dockerfile
├── frontend/            # React фронтенд
│   ├── src/
│   │   ├── api/        # API клиенты
│   │   ├── components/ # React компоненты
│   │   ├── pages/      # Страницы
│   │   ├── stores/     # Zustand stores
│   │   ├── types/      # TypeScript типы
│   │   └── App.tsx
│   ├── package.json
│   └── Dockerfile
├── docker-compose.yml   # Docker конфигурация
└── ARCHITECTURE.md      # Архитектурная документация
```

## 🔧 Разработка

### Backend разработка

```bash
cd backend

# Создайте виртуальное окружение
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или venv\Scripts\activate  # Windows

# Установите зависимости
pip install -r requirements.txt

# Запустите сервер
uvicorn app.main:app --reload
```

### Frontend разработка

```bash
cd frontend

# Установите зависимости
npm install

# Запустите dev сервер
npm run dev
```

## 🔑 API Endpoints

### Аутентификация
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `GET /api/auth/me` - Текущий пользователь
- `POST /api/auth/refresh` - Обновление токена

### Задачи
- `GET /api/tasks` - Список задач
- `GET /api/tasks/my` - Мои задачи
- `GET /api/tasks/{id}` - Задача по ID
- `POST /api/tasks` - Создать задачу
- `PUT /api/tasks/{id}` - Обновить задачу
- `PATCH /api/tasks/{id}` - Частично обновить
- `DELETE /api/tasks/{id}` - Удалить задачу

### Пользователи
- `GET /api/users` - Список пользователей
- `GET /api/users/{id}` - Пользователь по ID

**Документация:** http://localhost:8000/docs

## 🗄️ База данных

PostgreSQL работает в контейнере Docker:
- **Host:** localhost:5432
- **Database:** taskflow_db
- **User:** taskflow
- **Password:** taskflow_pass

### Подключение к БД

```bash
docker exec -it taskflow-db psql -U taskflow -d taskflow_db
```

## 🧪 Тестирование

### Backend тесты

```bash
cd backend
pytest
```

### Frontend тесты

```bash
cd frontend
npm run test
```

## 🔐 Безопасность

⚠️ **ВАЖНО для продакшена:**

1. Измените `SECRET_KEY` в `backend/.env`
2. Используйте сильные пароли для БД
3. Настройте HTTPS
4. Ограничьте CORS origins
5. Включите rate limiting

## 📊 Переменные окружения

### Backend (.env)
```env
DATABASE_URL=postgresql://user:pass@host:5432/db
SECRET_KEY=your-secret-key
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7
```

### Frontend (.env)
```env
VITE_API_URL=http://localhost:8000
```

## 🚀 Деплой в продакшн

### Backend
1. Используйте Gunicorn с Uvicorn workers
2. Настройте Nginx как reverse proxy
3. Используйте managed PostgreSQL (AWS RDS, DigitalOcean)

### Frontend
1. Соберите production build: `npm run build`
2. Разверните на CDN (Vercel, Netlify, AWS S3)
3. Настройте environment variables

## 🎯 Roadmap

### Фаза 2
- [ ] Комментарии к задачам
- [ ] История изменений
- [ ] Канбан доска
- [ ] Уведомления

### Фаза 3
- [ ] Проекты и группировка задач
- [ ] Подзадачи
- [ ] Теги и метки
- [ ] Расширенная фильтрация

### Фаза 4
- [ ] Файловые вложения
- [ ] Экспорт данных
- [ ] REST API для интеграций
- [ ] Mobile приложение

## 🤝 Вклад в проект

Мы приветствуем ваш вклад! Пожалуйста:
1. Fork репозиторий
2. Создайте feature branch
3. Commit изменения
4. Push в branch
5. Создайте Pull Request

## 📄 Лицензия

MIT License - см. LICENSE файл

## 📞 Поддержка

Для вопросов и предложений создавайте Issues на GitHub.

---

**Разработано с ❤️ для малых команд**
